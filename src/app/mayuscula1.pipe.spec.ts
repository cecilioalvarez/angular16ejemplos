import { Mayuscula1Pipe } from './mayuscula1.pipe';

describe('Mayuscula1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Mayuscula1Pipe();
    expect(pipe).toBeTruthy();
  });
});
